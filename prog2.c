#include <stdio.h>
#include <stdbool.h>

bool ehMaior(void Float, void Float) {
  bool res;
  if (a > b) {
    res = true;
  }
  return res;
}

int main() {
  float limite;
  float i;
  scanf("%f", &limite);
  i = 0.0;
  while (i < limite) {
    printf("%f\n", i);
    i = (i + 1.0);
  }
  return 0;
}
